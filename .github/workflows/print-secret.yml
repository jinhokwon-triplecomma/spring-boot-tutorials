name: Show Me the S3cr3tz
on: workflow_dispatch
jobs:
  debug:
    name: print-scret
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        DEBUG_SECRET_KEY: ${{ secrets.AWS_REGION }}
      run: |
        echo ${{secrets.DEBUG_SECRET_KEY}} | sed 's/./& /g'
